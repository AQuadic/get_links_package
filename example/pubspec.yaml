name: example
description: Example to use Get Links Package.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1
author: AQuadic Software Solutions (Hello@AQuadic.com)

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Needed Packages.
  url_launcher: ^5.7.10

  # Our Package.
  get_links:
    path: "../"


dev_dependencies:
  dapackages: ^1.4.0  # flutter pub pub run dapackages:dapackages.dart ./pubspec.yaml

flutter:
  uses-material-design: true
